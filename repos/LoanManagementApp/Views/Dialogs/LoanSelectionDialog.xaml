<Window x:Class="LoanManagementApp.Views.Dialogs.LoanSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Loan | قرض منتخب کریں"
        Width="450"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource DialogBackgroundBrush}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/DarkTheme.xaml"/>
                <ResourceDictionary Source="/Themes/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Border Grid.Row="0"
                Background="{DynamicResource HeaderBackgroundBrush}"
                Padding="24,16">
			<TextBlock Text="📋 Select Loan | قرض منتخب کریں"
                       FontSize="{DynamicResource FontSizeLarge}"
                       FontWeight="Bold"
                       Foreground="White"/>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" Margin="24,20,24,20" MaxHeight="300">
            <ListBox x:Name="LoansListBox"
                     ItemsSource="{Binding Loans}"
                     DisplayMemberPath="DisplayName"
                     SelectedItem="{Binding SelectedLoan}"
                     Style="{StaticResource AppListBoxStyle}"
                     ItemContainerStyle="{StaticResource AppListBoxItemStyle}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="12,8">
                            <StackPanel>
                                <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="{Binding DateRange}" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>

        <!-- Footer Buttons -->
        <Border Grid.Row="2"
                Background="{DynamicResource HeaderBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="24,14">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="✅ Select"
                        Click="SelectButton_Click"
                        Style="{DynamicResource PrimaryButtonStyle}"
                        Margin="0,0,8,0"
                        Padding="20,8"/>
                <Button Content="✖ Cancel"
                        Click="CancelButton_Click"
                        Style="{DynamicResource SecondaryButtonStyle}"
                        Padding="20,8"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>