<Window x:Class="LoanManagementApp.Views.Dialogs.PaymentDetailDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Payment Details | ادائیگی کی تفصیلات"
        Width="500"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource DialogBackgroundBrush}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/DarkTheme.xaml"/>
                <ResourceDictionary Source="/Themes/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Border Grid.Row="0"
                Background="{DynamicResource HeaderBackgroundBrush}"
                Padding="24,16">
			<TextBlock Text="🔍 Payment Details | ادائیگی کی تفصیلات"
                       FontSize="{DynamicResource FontSizeLarge}"
                       FontWeight="Bold"
                       Foreground="White"/>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="24,20,24,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Row 0: Installment Number -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Installment # | قسط نمبر:" Style="{StaticResource FormLabelStyle}" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding InstallmentNumber}" Style="{StaticResource ValueTextStyle}" Margin="8,0,0,0"/>

                <!-- Row 1: Payment Date -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Payment Date | تاریخ:" Style="{StaticResource FormLabelStyle}" VerticalAlignment="Center" Margin="0,8,0,0"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding PaymentDateDisplay}" Style="{StaticResource ValueTextStyle}" Margin="8,8,0,0"/>

                <!-- Row 2: Paid Amount -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Paid Amount | ادا شدہ رقم:" Style="{StaticResource FormLabelStyle}" VerticalAlignment="Center" Margin="0,8,0,0"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding PaidAmountDisplay}" Style="{StaticResource ValueTextStyle}" Margin="8,8,0,0" FontWeight="SemiBold" Foreground="{DynamicResource SuccessForegroundBrush}"/>

                <!-- Row 3: Balance Before -->
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Balance Before | پہلے باقی:" Style="{StaticResource FormLabelStyle}" VerticalAlignment="Center" Margin="0,8,0,0"/>
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding BalanceBeforeDisplay}" Style="{StaticResource ValueTextStyle}" Margin="8,8,0,0"/>

                <!-- Row 4: Balance After -->
                <TextBlock Grid.Row="4" Grid.Column="0" Text="Balance After | بعد میں باقی:" Style="{StaticResource FormLabelStyle}" VerticalAlignment="Center" Margin="0,8,0,0"/>
                <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding BalanceAfterDisplay}" Style="{StaticResource ValueTextStyle}" Margin="8,8,0,0"/>

                <!-- Row 5: Payment Type -->
                <TextBlock Grid.Row="5" Grid.Column="0" Text="Payment Type | قسم:" Style="{StaticResource FormLabelStyle}" VerticalAlignment="Center" Margin="0,8,0,0"/>
                <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding PaymentTypeDisplay}" Style="{StaticResource ValueTextStyle}" Margin="8,8,0,0"/>

                <!-- Row 6: Payment Method -->
                <TextBlock Grid.Row="6" Grid.Column="0" Text="Payment Method | طریقہ:" Style="{StaticResource FormLabelStyle}" VerticalAlignment="Center" Margin="0,8,0,0"/>
                <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding PaymentMethod}" Style="{StaticResource ValueTextStyle}" Margin="8,8,0,0" TextWrapping="Wrap"/>

                <!-- Row 7: Received By -->
                <TextBlock Grid.Row="7" Grid.Column="0" Text="Received By | وصول کنندہ:" Style="{StaticResource FormLabelStyle}" VerticalAlignment="Center" Margin="0,8,0,0"/>
                <TextBlock Grid.Row="7" Grid.Column="1" Text="{Binding ReceivedBy}" Style="{StaticResource ValueTextStyle}" Margin="8,8,0,0"/>

                <!-- Row 8: Notes -->
                <TextBlock Grid.Row="8" Grid.Column="0" Text="Notes | نوٹس:" Style="{StaticResource FormLabelStyle}" VerticalAlignment="Top" Margin="0,8,0,0"/>
                <TextBlock Grid.Row="8" Grid.Column="1" Text="{Binding Notes}" Style="{StaticResource ValueTextStyle}" Margin="8,8,0,0" TextWrapping="Wrap"/>

                <!-- Row 9: Verified Status -->
                <TextBlock Grid.Row="9" Grid.Column="0" Text="Verified | تصدیق شدہ:" Style="{StaticResource FormLabelStyle}" VerticalAlignment="Center" Margin="0,8,0,0"/>
                <TextBlock Grid.Row="9" Grid.Column="1" Text="{Binding VerifiedStatus}" Style="{StaticResource ValueTextStyle}" Margin="8,8,0,0"/>
            </Grid>
        </ScrollViewer>

        <!-- Footer Close Button -->
        <Border Grid.Row="2"
                Background="{DynamicResource HeaderBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="24,14">
            <Button Content="✖ Close"
                    Click="CloseButton_Click"
                    Style="{DynamicResource SecondaryButtonStyle}"
                    HorizontalAlignment="Right"
                    MinWidth="100"/>
        </Border>
    </Grid>
</Window>