<Window x:Class="LoanManagementApp.Views.Dialogs.PinVerificationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PIN Verification | پن تصدیق"
        Width="380"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource DialogBackgroundBrush}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/DarkTheme.xaml"/>
                <ResourceDictionary Source="/Themes/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Border Grid.Row="0"
                Background="{DynamicResource HeaderBackgroundBrush}"
                Padding="24,16">
            <TextBlock Text="🔐 PIN Verification Required | پن تصدیق ضروری ہے"
                       FontSize="{DynamicResource FontSizeLarge}"
                       FontWeight="Bold"
                       Foreground="White"/>
        </Border>

        <!-- Content -->
        <StackPanel Grid.Row="1" Margin="24,20,24,20">
            <TextBlock Text="Please enter your PIN to proceed | براہ کرم اپنا پن درج کریں"
                       Style="{DynamicResource FormLabelStyle}"
                       Margin="0,0,0,8"/>
            <PasswordBox x:Name="PinPasswordBox"
                         Style="{DynamicResource AppPasswordBoxStyle}"
                         Margin="0,0,0,12"/>
            <TextBlock x:Name="ErrorText"
                       Foreground="{DynamicResource DangerBrush}"
                       FontSize="12"
                       Margin="0,0,0,0"
                       Visibility="Collapsed"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Footer Buttons -->
        <Border Grid.Row="2"
                Background="{DynamicResource HeaderBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="24,14">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="✅ Verify"
                        Click="VerifyButton_Click"
                        Style="{DynamicResource PrimaryButtonStyle}"
                        Margin="0,0,8,0"
                        Padding="20,8"/>
                <Button Content="✖ Cancel"
                        Click="CancelButton_Click"
                        Style="{DynamicResource SecondaryButtonStyle}"
                        Padding="20,8"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>